# ── Python ────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/

# ── 가상환경 ──────────────────────────────────────
venv/
env/
.venv/
ENV/

# ── IDE ───────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.cursorignore

# ── Jupyter ───────────────────────────────────────
.ipynb_checkpoints/

# ── OS ────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── 데이터셋 (용량 큼, 학습용) ────────────────────
data/raw/
data/processed/
data/interim/
*.npy
*.pkl
!models/motility_ensemble.pkl

# ── 학습 결과물 ───────────────────────────────────
runs/
wandb/
lightning_logs/

# ── 영상 / 결과물 ─────────────────────────────────
*.mp4
*.avi
*.mov
*.mkv
tmp_uploads/*
!tmp_uploads/.gitkeep
outputs/*
!outputs/.gitkeep

# ── 모델 가중치 (용량 큼, 별도 배포) ─────────────
*.pt
*.pth
*.h5
*.onnx
*.pkl
models/
!models/.gitkeep
!models/*.json

# ── 문서 임시파일 ─────────────────────────────────
*.bak
*.tmp
~$*

# ── 환경 변수 ─────────────────────────────────────
.env
*.local

# ── ngrok ─────────────────────────────────────────
ngrok.exe
ngrok.yml

# ── 논문 실험 산출물 (그림 PNG·결과 영상은 코드만 공유) ──
paper/figures/**/*.png
paper/experiments/**/*.mp4